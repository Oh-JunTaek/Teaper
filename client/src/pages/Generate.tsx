import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { CheckCircle2, Loader2, Sparkles, TriangleAlert } from "lucide-react";
import { FormEvent, useState } from "react";
import { toast } from "sonner";
import { useLocation } from "wouter";

export default function Generate() {
  const [, setLocation] = useLocation(); const [form, setForm] = useState({ subject: "화학 I", unit: "", difficulty: "중", questionType: "자료 분석형", points: 3, questionCount: 1, additionalRequirements: "" });
  const create = trpc.assessment.generation.create.useMutation({ onSuccess: result => { toast.success(`${result.questionIds.length}개 문항 초안이 생성되었습니다.`); setLocation("/review"); }, onError: error => toast.error(error.message) });
  const submit = (event: FormEvent) => { event.preventDefault(); create.mutate(form); };
  return <div className="mx-auto max-w-5xl"><div className="text-center"><Badge className="bg-[#E6F4EE] text-[#15856B] hover:bg-[#E6F4EE]">근거 기반 문항 초안</Badge><h1 className="mt-3 text-3xl font-bold text-[#183248]">문항 생성 요청</h1><p className="mt-2 text-slate-500">등록된 교육과정·기출문제·출제 지침을 바탕으로 문항 초안을 생성합니다.</p></div><section className="mt-8 grid gap-5 lg:grid-cols-[1fr_300px]"><form onSubmit={submit} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-bold text-[#183248]">출제 조건</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><div><Label>과목</Label><Input value={form.subject} onChange={e => setForm({ ...form, subject: e.target.value })} className="mt-1.5" /></div><div><Label>단원</Label><Input required value={form.unit} onChange={e => setForm({ ...form, unit: e.target.value })} placeholder="예: 화학 결합" className="mt-1.5" /></div><div><Label>난이도</Label><select value={form.difficulty} onChange={e => setForm({ ...form, difficulty: e.target.value })} className="mt-1.5 h-10 w-full rounded-md border border-input bg-background px-3 text-sm"><option>하</option><option>중</option><option>상</option></select></div><div><Label>문제 유형</Label><select value={form.questionType} onChange={e => setForm({ ...form, questionType: e.target.value })} className="mt-1.5 h-10 w-full rounded-md border border-input bg-background px-3 text-sm">{["개념 확인형", "자료 분석형", "계산형", "그래프 해석형", "실험 자료형", "비교·추론형", "상황 적용형"].map(item => <option key={item}>{item}</option>)}</select></div><div><Label>배점</Label><Input type="number" min="1" max="20" value={form.points} onChange={e => setForm({ ...form, points: Number(e.target.value) })} className="mt-1.5" /></div><div><Label>문항 수 <span className="text-xs font-normal text-slate-400">(최대 5개)</span></Label><Input type="number" min="1" max="5" value={form.questionCount} onChange={e => setForm({ ...form, questionCount: Number(e.target.value) })} className="mt-1.5" /></div><div className="sm:col-span-2"><Label>추가 요구사항 <span className="text-xs font-normal text-slate-400">(선택)</span></Label><Textarea value={form.additionalRequirements} onChange={e => setForm({ ...form, additionalRequirements: e.target.value })} className="mt-1.5 min-h-28" placeholder="예: 결합의 극성과 분자 모양의 관계를 판단하는 자료를 포함해 주세요." /></div></div><Button disabled={create.isPending} className="mt-6 h-12 w-full rounded-xl bg-[#15856B] text-base hover:bg-[#106C58]">{create.isPending ? <><Loader2 className="mr-2 h-5 w-5 animate-spin" />근거 확인·초안 작성·검증 중</> : <><Sparkles className="mr-2 h-5 w-5" />문항 초안 생성</>}</Button></form><aside className="space-y-4"><div className="rounded-2xl border border-[#B9DCCF] bg-[#F2FBF6] p-5"><CheckCircle2 className="h-5 w-5 text-[#15856B]" /><h2 className="mt-3 font-bold text-[#183248]">생성 전 확인</h2><ul className="mt-3 space-y-2 text-sm leading-5 text-slate-600"><li>동일 과목·단원의 참고 자료 또는 기출문제가 필요합니다.</li><li>교육과정·기출·지침을 함께 확인해 초안을 만듭니다.</li><li>기출문제는 복제하지 않고 유사도 검사를 통과해야 합니다.</li></ul></div><div className="rounded-2xl border border-[#F3D6A3] bg-[#FFF9EC] p-5"><TriangleAlert className="h-5 w-5 text-[#B56716]" /><h2 className="mt-3 font-bold text-[#183248]">교사 최종 검수</h2><p className="mt-2 text-sm leading-6 text-slate-600">자동 검증이 통과한 문항도 반드시 근거와 해설을 확인한 후 승인하세요.</p></div></aside></section></div>;
}
